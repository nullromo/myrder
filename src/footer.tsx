import { myrderBlue } from './colors';

export const Footer = () => {
    return (
        <div
            style={{
                background: myrderBlue,
                color: 'white',
                display: 'flex',
                justifyContent: 'center',
                width: '100%',
            }}
        >
            asdf
        </div>
    );
};
