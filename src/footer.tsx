import { myrderBlue } from './colors';

export const Footer = () => {
    return (
        <div
            style={{
                display: 'flex',
                width: '100%',
                justifyContent: 'center',
                color: 'white',
                background: myrderBlue,
            }}
        >
            asdf
        </div>
    );
};
